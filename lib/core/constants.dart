class AppConstants {
  static const String baseUrl = 'https://pwa-api-eavx.onrender.com';

  /// When true, the app runs as a pure front-end using in-memory mock data
  /// (see [MockData]) and a default login — no backend required.
  static const bool useMockData = false;

  static const double wearableBreakpoint = 320.0;
  static const double tabletBreakpoint = 768.0;
}
